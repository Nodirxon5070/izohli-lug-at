spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/izohli_lugat_db
    username: izohli_lugat_user
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true